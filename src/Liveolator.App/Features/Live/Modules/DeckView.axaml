<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:m="clr-namespace:Liveolator.App.Features.Live.Modules"
             xmlns:controls="clr-namespace:Liveolator.App.Controls"
             x:Class="Liveolator.App.Features.Live.Modules.DeckView"
             x:DataType="m:DeckViewModel">

  <Border Classes="panel">
    <StackPanel Margin="18" Spacing="14">

      <!-- header: badge · title · menu -->
      <Grid ColumnDefinitions="Auto,*,Auto">
        <Border Grid.Column="0" Width="36" Height="36" CornerRadius="10" VerticalAlignment="Center">
          <Border.Background>
            <LinearGradientBrush StartPoint="0%,0%" EndPoint="0%,100%">
              <GradientStop Color="#2E74E6" Offset="0" />
              <GradientStop Color="#1A4FB8" Offset="1" />
            </LinearGradientBrush>
          </Border.Background>
          <TextBlock Foreground="White" FontWeight="SemiBold" FontSize="15"
                     HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding DeckId}" />
        </Border>
        <StackPanel Grid.Column="1" Spacing="1" Margin="12,0,0,0" VerticalAlignment="Center">
          <TextBlock FontSize="15" FontWeight="SemiBold" Text="{Binding DeckId, StringFormat='Deck {0}'}" />
          <TextBlock Classes="faint" Text="{Binding Title}" />
        </StackPanel>
        <Button Grid.Column="2" Classes="icon" Content="•••" FontSize="11" IsEnabled="False" />
      </Grid>

      <!-- waveform placeholder (no live waveform backend yet) -->
      <Border Height="56" Background="{StaticResource S2}" BorderBrush="{StaticResource Hair}"
              BorderThickness="1" CornerRadius="8" ClipToBounds="True">
        <Grid>
          <controls:WaveformStrip />
          <Border HorizontalAlignment="Center" VerticalAlignment="Center"
                  Background="{StaticResource S2}" CornerRadius="4" Padding="8,3">
            <TextBlock Classes="faint" Text="Waveform — coming soon" />
          </Border>
        </Grid>
      </Border>

      <!-- hot cues (disabled — no handler yet) -->
      <UniformGrid Columns="4">
        <Button Classes="key" Content="1" IsEnabled="{Binding CanHotCue}" Margin="0,0,4,0" />
        <Button Classes="key" Content="2" IsEnabled="{Binding CanHotCue}" Margin="0,0,4,0" />
        <Button Classes="key" Content="3" IsEnabled="{Binding CanHotCue}" Margin="0,0,4,0" />
        <Button Classes="key" Content="4" IsEnabled="{Binding CanHotCue}" />
      </UniformGrid>

      <!-- transport: CUE (disabled) · Play · Loop (disabled) · Sync (disabled) -->
      <UniformGrid Columns="4">
        <Button Classes="key" Content="CUE" IsEnabled="{Binding CanCue}" Margin="0,0,4,0" />
        <Button Classes="play" Classes.on="{Binding IsPlaying}"
                Command="{Binding PlayPauseCommand}" Margin="0,0,4,0">
          <Path Data="M 0,0 L 13,8 L 0,16 Z" Fill="{StaticResource Accent}" />
        </Button>
        <Button Classes="key" Content="LOOP" IsEnabled="{Binding CanLoop}" Margin="0,0,4,0" />
        <Button Classes="key" Content="SYNC" IsEnabled="{Binding CanSync}" />
      </UniformGrid>

      <!-- EQ / filter / pitch knobs -->
      <UniformGrid Columns="5">
        <StackPanel Spacing="6" HorizontalAlignment="Center">
          <controls:Knob Width="56" Height="56" HorizontalAlignment="Center"
                         Value="{Binding EqHigh.Value}" IsEnabled="{Binding EqHigh.IsEnabled}" />
          <Grid ColumnDefinitions="*,*" Width="62">
            <TextBlock Classes="tick" Text="−∞" />
            <TextBlock Grid.Column="1" Classes="tick" HorizontalAlignment="Right" Text="+6" />
          </Grid>
          <TextBlock Classes="faint" HorizontalAlignment="Center" Text="HI" />
        </StackPanel>
        <StackPanel Spacing="6" HorizontalAlignment="Center">
          <controls:Knob Width="56" Height="56" HorizontalAlignment="Center"
                         Value="{Binding EqMid.Value}" IsEnabled="{Binding EqMid.IsEnabled}" />
          <Grid ColumnDefinitions="*,*" Width="62">
            <TextBlock Classes="tick" Text="−∞" />
            <TextBlock Grid.Column="1" Classes="tick" HorizontalAlignment="Right" Text="+6" />
          </Grid>
          <TextBlock Classes="faint" HorizontalAlignment="Center" Text="MID" />
        </StackPanel>
        <StackPanel Spacing="6" HorizontalAlignment="Center">
          <controls:Knob Width="56" Height="56" HorizontalAlignment="Center"
                         Value="{Binding EqLow.Value}" IsEnabled="{Binding EqLow.IsEnabled}" />
          <Grid ColumnDefinitions="*,*" Width="62">
            <TextBlock Classes="tick" Text="−∞" />
            <TextBlock Grid.Column="1" Classes="tick" HorizontalAlignment="Right" Text="+6" />
          </Grid>
          <TextBlock Classes="faint" HorizontalAlignment="Center" Text="LOW" />
        </StackPanel>
        <StackPanel Spacing="6" HorizontalAlignment="Center">
          <controls:Knob Width="56" Height="56" HorizontalAlignment="Center"
                         Value="{Binding Filter.Value}" IsEnabled="{Binding Filter.IsEnabled}" />
          <Grid ColumnDefinitions="*,*" Width="62">
            <TextBlock Classes="tick" Text="−∞" />
            <TextBlock Grid.Column="1" Classes="tick" HorizontalAlignment="Right" Text="+6" />
          </Grid>
          <TextBlock Classes="faint" HorizontalAlignment="Center" Text="FLT" />
        </StackPanel>
        <StackPanel Spacing="6" HorizontalAlignment="Center">
          <controls:Knob Width="56" Height="56" HorizontalAlignment="Center"
                         Value="{Binding Pitch.Value}" IsEnabled="{Binding Pitch.IsEnabled}" />
          <Grid ColumnDefinitions="*,*" Width="62">
            <TextBlock Classes="tick" Text="−100%" />
            <TextBlock Grid.Column="1" Classes="tick" HorizontalAlignment="Right" Text="+100%" />
          </Grid>
          <TextBlock Classes="faint" HorizontalAlignment="Center" Text="PITCH" />
        </StackPanel>
      </UniformGrid>
    </StackPanel>
  </Border>
</UserControl>

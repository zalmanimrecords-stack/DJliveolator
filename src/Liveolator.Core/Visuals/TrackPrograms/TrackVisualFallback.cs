namespace Liveolator.Core.Visuals.TrackPrograms;

/// <summary>Output used when a track has no playable visual cue.</summary>
public enum TrackVisualFallback
{
    Transparent,
    SolidColor,
    GlobalDefaultProgram,
}

feat(enrichment): online BPM/key module — Core + Online binding + MCP tool (doc 16)

Build the offline-first online metadata enrichment that doc 16 designed and deferred.

Core (pure, tested): IMetadataProvider seam, TrackLookupQuery, OnlineTrackMetadata,
IAudioFingerprinter + AudioFingerprint, and MetadataMergePolicy — local detection stays
authoritative; agreement (incl. half/double-time) cross-checks and promotes to Ok,
disagreement keeps local and flags PartiallyAnalyzed, local-missing fills from online.

Liveolator.Online (new binding, HTTP behind injected HttpClient): AcoustIdClient
(fingerprint to identity), GetSongBpmClient (tempo/key/genre), OnlineMetadataProvider
(fingerprint to identity to GetSongBPM, tag fallback), FpcalcFingerprinter and
FpcalcOutputParser (Chromaprint CLI). Every miss/error resolves to null (never throws).

Liveolator.Mcp: lookup_track_online tool + OnlineLookupResult (with mandatory GetSongBPM
attribution), ServerConfig --getsongbpm-key/--acoustid-key/--fpcalc (+ LIVEOLATOR_* env),
DI wiring, and a DisabledMetadataProvider that reports "not configured" without a key.

ToS researched (distributed app): GetSongBPM (free, attribution-backlink required) for
tempo/key; AcoustID+MusicBrainz for identity. Spotify audio-features and Beatport/Tunebat
rejected. +29 tests (Core 6, Online 23). docs/16 updated deferred -> BUILT.

Pending live activation (external): API keys + fpcalc binary, and the Libraries "Fetch
online" button. Snapshot also folds in concurrent multi-agent work (deck sync/pitch/cue,
output-device/buffer application, UI). Also gitignore .claude/worktrees/.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
